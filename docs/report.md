# AUJava → C Transpiler — Project Report

> Placeholder — written incrementally across the project and finalized in Phase 12.

## Design assumptions (to document)

- `println` on `boolean` prints `true` / `false`.
- Subclass objects are assignable to a parent-typed variable (upcast allowed).
- Field initializers / default values.
- Overriding methods must keep the same signature.
